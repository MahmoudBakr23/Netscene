import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  root: {
    display: 'flex',
    height: '100%',
  },
  content: {
    display: 'flex',
  },
  toolbar: {
    display: 'flex',
  },
}));

export default useStyles;
